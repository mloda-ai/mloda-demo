[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-demo"
version = "0.1.0"
description = "Live demos built on mloda: credit risk with XAI attribution, and Physical AI sensor features with OpenTelemetry traces and OpenLineage pictures."
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "tom@mloda.ai" }]
dependencies = [
    "mloda>=0.13.0,<0.14.0",
    "mloda-community-otel>=0.4.7",
    "mloda-community-openlineage>=0.4.7",
    "opentelemetry-sdk>=1.30,<2",
    "marimo",
    "pandas",
    "numpy",
    "matplotlib",
    "pillow",
]
requires-python = ">=3.10"

[project.scripts]
mloda-demo = "mloda_demo.cli:main"

[project.optional-dependencies]
credit-risk = [
    "torch",
    "zennit",
    "openpyxl",
]
dev = [
    "mloda-testing>=0.4.7",
    "openml",
    "tox",
    "tox-uv",
    "pytest",
    "ruff",
    "mypy",
    "bandit",
]

# Load spans, declared span attributes and step-level OpenLineage inputs are not released yet.
# 816953a is the last registry commit that still pins mloda 0.13; switch back to PyPI after the next release.
[tool.uv.sources]
mloda-community-otel = { git = "https://github.com/mloda-ai/mloda-registry", rev = "816953a7c9a451e2c2ce65d1340cd09d5be59056", subdirectory = "mloda/community/extenders/otel" }
mloda-community-openlineage = { git = "https://github.com/mloda-ai/mloda-registry", rev = "816953a7c9a451e2c2ce65d1340cd09d5be59056", subdirectory = "mloda/community/extenders/openlineage" }

[tool.uv]
override-dependencies = [
    "mloda-community-extenders-shared @ git+https://github.com/mloda-ai/mloda-registry@816953a7c9a451e2c2ce65d1340cd09d5be59056#subdirectory=mloda/community/extenders/shared",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["mloda_demo*"]

# notebooks/physical_ai.css styles the light theme only; the code font is sized for a projector.
[tool.marimo.display]
theme = "light"
code_editor_font_size = 18

[tool.pytest.ini_options]
testpaths = ["mloda_demo", "tests"]
python_files = ["test_*.py"]
markers = [
    "slow: integration tests that load TabPFN / run attribution (deselect with -m 'not slow')",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint.per-file-ignores]
# marimo writes a bare `return` at the end of every cell.
"notebooks/*" = ["PLR1711"]

[tool.mypy]
strict = true
ignore_missing_imports = true
namespace_packages = true
explicit_package_bases = true
exclude = [
    "^scripts/",
    "^demo/",
    "^notebooks/",
    "^\\.tox/",
    "^\\.venv/",
]

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

[tool.bandit]
exclude_dirs = [".tox", ".venv", "tests"]

