[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-demo"
version = "0.1.0"
description = "Live demos built on mloda: credit risk with XAI attribution, and Physical AI sensor features with OpenTelemetry traces (in progress)."
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "tom@mloda.ai" }]
dependencies = [
    "mloda>=0.13.0,<0.14.0",
    "mloda-community-otel>=0.4.7",
    "opentelemetry-sdk>=1.30,<2",
    "marimo",
    "pandas",
    "numpy",
    "matplotlib",
]
requires-python = ">=3.10"

[project.scripts]
mloda-demo = "mloda_demo.cli:main"

[project.optional-dependencies]
credit-risk = [
    "torch",
    "zennit",
    "openpyxl",
]
dev = [
    "mloda-testing>=0.4.7",
    "openml",
    "tox",
    "tox-uv",
    "pytest",
    "ruff",
    "mypy",
    "bandit",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["mloda_demo*"]

[tool.pytest.ini_options]
testpaths = ["mloda_demo", "tests"]
python_files = ["test_*.py"]
markers = [
    "slow: integration tests that load TabPFN / run attribution (deselect with -m 'not slow')",
]

[tool.ruff]
line-length = 120

[tool.mypy]
strict = true
ignore_missing_imports = true
namespace_packages = true
explicit_package_bases = true
exclude = [
    "^scripts/",
    "^demo/",
    "^\\.tox/",
    "^\\.venv/",
]

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

[tool.bandit]
exclude_dirs = [".tox", ".venv", "tests"]

