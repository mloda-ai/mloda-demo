[build-system]
requires = ["setuptools>=84.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mloda-demo"
version = "0.1.0"
description = "Live demos built on mloda: credit risk with XAI attribution, and Physical AI sensor features with OpenTelemetry traces."
license = "Apache-2.0"
authors = [{ name = "Tom Kaltofen", email = "tom@mloda.ai" }]
dependencies = [
    "mloda>=0.13.0,<0.14.0",
    "mloda-community-otel>=0.4.7",
    "opentelemetry-sdk>=1.30,<2",
    "marimo",
    "pandas",
    "numpy",
    "matplotlib",
]
requires-python = ">=3.10"

[project.scripts]
mloda-demo = "mloda_demo.cli:main"

[project.optional-dependencies]
credit-risk = [
    "torch",
    "zennit",
    "openpyxl",
]
dev = [
    "mloda-testing>=0.4.7",
    "openml",
    "tox",
    "tox-uv",
    "pytest",
    "ruff",
    "mypy",
    "bandit",
]

# Load spans and declared span attributes are not released yet; switch back to PyPI after the next registry release.
[tool.uv.sources]
mloda-community-otel = { git = "https://github.com/mloda-ai/mloda-registry", rev = "bf2c7b19318cae8fb9a4bea24d7be6a090956550", subdirectory = "mloda/community/extenders/otel" }

[tool.uv]
override-dependencies = [
    "mloda-community-extenders-shared @ git+https://github.com/mloda-ai/mloda-registry@bf2c7b19318cae8fb9a4bea24d7be6a090956550#subdirectory=mloda/community/extenders/shared",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["mloda_demo*"]

[tool.pytest.ini_options]
testpaths = ["mloda_demo", "tests"]
python_files = ["test_*.py"]
markers = [
    "slow: integration tests that load TabPFN / run attribution (deselect with -m 'not slow')",
]

[tool.ruff]
line-length = 120

[tool.ruff.lint.per-file-ignores]
# marimo writes a bare `return` at the end of every cell.
"notebooks/*" = ["PLR1711"]

[tool.mypy]
strict = true
ignore_missing_imports = true
namespace_packages = true
explicit_package_bases = true
exclude = [
    "^scripts/",
    "^demo/",
    "^notebooks/",
    "^\\.tox/",
    "^\\.venv/",
]

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true

[tool.bandit]
exclude_dirs = [".tox", ".venv", "tests"]

