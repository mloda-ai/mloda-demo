Made up for the talk: `finance.csv` (eight invoices), `sales.csv` (six deals) and `applicant.json` (one credit request).
The history table is `../german_credit.csv`, the UCI German credit data via OpenML (see `scripts/fetch_german_credit.py`).
